#!/usr/bin/env sh
# Auto-bump the patch version on every commit.
# Uses pnpm (project's declared packageManager) and stages only the files
# that pnpm version touches — never unrelated working-tree changes.
pnpm --no-git-tag-version version patch &&
  git add package.json pnpm-lock.yaml
