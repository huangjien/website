services:
  web:
    build:
      context: ./
      target: runner
    command: ["node", "server.js"]
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production
      PORT: 8080
      HOSTNAME: 0.0.0.0
